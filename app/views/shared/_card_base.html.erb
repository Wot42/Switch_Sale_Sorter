
<!-- <%= render :partial => 'card_base', locals: { game: @game,  } %>  -->

<div class="card">
  <%= link_to game_path(game) do %>
    <img src =<%= game.pic %> class="card-img-top">
  <% end %>
  <div class="card-body d-flex w-100 justify-content-between flex-column">
    <div class="w-100 d-flex">
      <h5 class="card-title mb-auto"><%= game.title %></h5>
    </div>
    <br>
    <div class="w-100 d-flex flex-column">
      <div class="w-100 d-flex justify-content-between">
        <p class="card-text"><%= book.price %></p>
        <p class="card-text">£<%= book.sale_price %></p>
      </div>
      <div class="w-100 d-flex justify-content-around">
        <%= link_to "Select", game_path(game), class: "text-decoration-none btn" %>
      </div>
    </div>
  </div>
</div>
